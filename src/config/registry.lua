local registry = {}

registry.locales = {
  { id = "en", name = "English" },
}

registry.presets = {
  { id = "default", name = "Default" },
  { id = "minimal", name = "Minimal" },
}

return registry
